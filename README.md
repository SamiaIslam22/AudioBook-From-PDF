# AudioBook-From-PDF
This is my first project on python on converting a PDF Book to a Audio Book and allowing the user to select the number of pages.
First install **pip install pyttsx3** and **pip install PyPDF2** in the terminal
Followingly, after running the code, select the pdf file that you would like to convert to a audiobook and select the pages you would like to listen to. 
